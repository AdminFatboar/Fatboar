
pipeline {
    agent any

    environment {
        NODE_ENV='test'
    }
    stages {
        stage('Build') {
            steps {
               
                sh "docker-compose --version"
          }
        }
    }
}